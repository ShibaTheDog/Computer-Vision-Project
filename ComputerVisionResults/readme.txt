ComputerVisionResults contains two files, BaseLineModel and patienceEpochAdjustedModel.
Inside of these files contain images and a folder. The images are the results from the Test data and the folder contains the 
train data. 


The train data also contains the YAML file that contains the trained model inside of it. 